export const styles = {
  card: {
    maxWidth: "400px",
    backdropFilter: "blur(12px)",
    background: "rgba(255, 255, 255, 0.15)",
    boxShadow: "0 8px 32px rgba(0, 0, 0, 0.2)",
  },
} as const; 